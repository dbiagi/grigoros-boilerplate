{% extends 'base.html.twig' %}

{% block title %}Home Page{% endblock %}

{% block content %}
    <div class="container">
        <h1>
            <a href="{{ path('silex_home') }}">Silex App</a>
        </h1>

        <div>
            <data-table :data="{{ users | json_encode() }}"></data-table>
        </div>
    </div>

    {% include 'Vue/data_table.html.twig' %}
{% endblock %}
